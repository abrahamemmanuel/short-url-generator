import { injectable } from "inversify";

@injectable()
export class LinksRepository {
  //methods to create
  //1. save short link, the short link should be the key

  //2. get long url using short link

  //3. get all links also accept search params at least 3 characters
}